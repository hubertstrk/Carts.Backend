var config = {
  accountName: '[account name]',
  accountKey: '[account key]',
  partitionKey: 'carts',
  tableNameCart: 'cart',
  tableNameTag: 'tag',
  tableNameUser: 'user'
}

exports.config = config